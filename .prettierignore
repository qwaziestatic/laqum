node_modules/
dist/
coverage/
pnpm-lock.yaml
*.tsbuildinfo

# The reference schema is a byte-for-byte copy of the reviewed DDL in the
# project brief. Formatting it would break the equality test in
# db/test/schema-copy.test.ts.
db/schema.sql

# Generated by kysely-codegen and checked in. `codegen:verify` compares the
# file against freshly generated output, so reformatting it fails CI.
db/src/generated.ts

# The project brief, stored verbatim as the authoritative spec. Prettier would
# rewrite its tables, lists and indentation, which would stop it being verbatim.
docs/BRIEF.md

# Written by Expo (Metro) on every start, in its own formatting, and ignored
# by apps/mobile/.gitignore. Prettier reads only the ROOT .gitignore, so it
# is listed here too, or format:check fails on any machine that ran Metro.
apps/mobile/expo-env.d.ts

# Written by scripts/brand/build.mjs, whose --check (CI) compares them byte
# for byte with what the one brand source produces. Never hand-formatted.
assets/brand/generated/
apps/dashboard/public/favicon.svg
apps/dashboard/src/brand/intro-wordmark.svg
