{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Typecheck-only config: it covers the vitest config, which must not be
    // emitted. tsconfig.build.json sets rootDir/outDir for the real build.
    "noEmit": true
  },
  "include": ["src/**/*.ts", "vitest.config.ts"]
}
